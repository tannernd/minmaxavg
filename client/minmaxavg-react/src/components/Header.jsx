const Header = (props) => {
  return (
    <header className="text-center">
      <h1>Find the Min, Max, and Average</h1>
      <a href={"/"}>Try the vanilla HTML/JS version</a>
    </header>
  );
};
export default Header;
